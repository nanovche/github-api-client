# github-api-client

program makes calls to github api -> reads in names from a file and 
makes calls to the api to retrieve various info(num of forks, activity, num of repos etc...)
prints result to console 
